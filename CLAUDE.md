# 작업 규칙

## 이미지 생성

- **힉스필드(Higgsfield) 이미지·영상 생성은 실행 전에 반드시 사용자에게 물어볼 것.**
  크레딧이 소모되는 유료 작업이므로, 장수와 예상 비용을 먼저 알리고 승인을 받은 뒤에 생성한다.
  `get_cost` 프리플라이트와 `balance` 조회는 비용이 들지 않으므로 미리 확인해도 된다.
- **인물이 등장하는 이미지는 모두 한국인으로 생성할 것.** 프롬프트에 한국인임을 명시한다.
- 실제 인물(예: 대표 강사 서보국)을 AI로 생성해 그 사람의 사진처럼 쓰지 않는다.
  인물 슬롯이 필요하면 얼굴이 드러나지 않는 일반 이미지로 만들고, 실제 촬영본으로 교체하도록 안내한다.
- AI로 만든 이미지는 임시·시안용임을 `images/README.md`에 명시해 둔다.

## 사이트

- 이 저장소는 다이브마루(경주 감포 스쿠버 다이빙 교육) 소개 랜딩페이지다.
- 금액·세부 일정은 노출하지 않고 상세 내용은 문의로 연결한다.
- 빌드 도구 없는 정적 파일 구성을 유지한다 (Vercel 배포).
